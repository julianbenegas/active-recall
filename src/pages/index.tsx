import Widget from 'components/widget'

const HomePage = () => (
  <div className="flex justify-center p-16">
    <Widget
      question="As a consequence, the net effect of any quantum circuit (before measurement) is to effect a unitary operation on the state space of the system."
      answer="Two"
      currentRepetition="in-text"
    />
  </div>
)

export default HomePage
